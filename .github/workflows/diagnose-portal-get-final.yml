name: Diagnose portal GET final
on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/diagnose-portal-get-final.yml'
permissions:
  contents: read
jobs:
  final:
    runs-on: ubuntu-latest
    steps:
      - run: echo final
