name: Diagnose portal GET trigger

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/diagnose-portal-get-trigger.yml'

permissions:
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo trigger
