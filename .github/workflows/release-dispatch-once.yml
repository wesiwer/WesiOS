name: Dispatch verified organization UX release once

on:
  push:
    branches:
      - agent/org-hierarchy-v1
    paths:
      - .github/workflows/release-dispatch-once.yml

permissions:
  actions: write
  contents: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/org-hierarchy-v1
          fetch-depth: 0

      - name: Dispatch canonical release workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run release-app.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref agent/org-hierarchy-v1 \
            -f notes='Улучшены права организаций: понятные русские настройки, пресеты доступа, управление из Контактов, фильтры и сортировка сотрудников по организациям и должностям.' \
            -f deploy_to_server=true

      - name: Self-remove dispatcher
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/release-dispatch-once.yml
          git config user.name 'WesiOS Automation'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git commit -m 'ci: remove release dispatcher [skip ci]'
          git push origin HEAD:agent/org-hierarchy-v1
