{
  "name": "wesi-connectors",
  "private": true,
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "check": "node --check server.mjs && node --check policy.mjs && node --check vault.mjs && node --check github.mjs",
    "test": "node --test *.test.mjs"
  }
}
