{
  "name": "wesi-finance-functions",
  "description": "Wesi Treasury — модуль финансов и прогнозирования баланса (WesiOS)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "node test/local-test.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions:forecastBalance,functions:nightlyForecastRefresh"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "private": true
}
